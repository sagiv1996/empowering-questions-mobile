query findRandomQuestionsByUserId($userId: ID!) {
  findRandomQuestionsByUserId(userId: $userId) {
    _id
    string
  }
}

mutation ($rank: Float!, $userId: ID!, $questionId: ID!) {
  rankQuestion(rank: $rank, userId: $userId, questionId: $questionId) {
    _id
    string
  }
}
